{
  "buildCommand": "npx esbuild api/_handler.ts --bundle --platform=node --format=esm --packages=external --outfile=api/handler.js && npm run build:client",
  "outputDirectory": "dist/app/client",
  "installCommand": "npm ci",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
